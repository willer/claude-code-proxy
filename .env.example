# API Keys for different providers
# Only include the keys for providers you plan to use
OPENAI_API_KEY="sk-..." # Required for OpenAI models (openai/*)
GEMINI_API_KEY="your-google-ai-studio-key" # Required for Gemini models (gemini/*)
ANTHROPIC_API_KEY="your-anthropic-api-key" # Required for Anthropic models (anthropic/*)

# Model mapping for Claude aliases (with provider prefixes)
# These values control which models are used when "haiku" or "sonnet" are requested
BIG_MODEL="openai/gpt-4.1" # Model to use for "sonnet" requests
SMALL_MODEL="openai/gpt-4.1-mini" # Model to use for "haiku" requests

# Example: Using Gemini models for Claude aliases
# BIG_MODEL="gemini/gemini-2.5-pro-preview-03-25"
# SMALL_MODEL="gemini/gemini-2.0-flash"

# Logging configuration (optional)
# LOGLEVEL="INFO" # Options: DEBUG, INFO, WARN, ERROR
# SHOW_MODEL_DETAILS="true" # Set to "true" to show detailed model information